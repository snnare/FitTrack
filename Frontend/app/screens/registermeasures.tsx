import { View, Text, TextInput, StyleSheet, Alert, ScrollView, TouchableOpacity } from 'react-native';
import { Formik } from 'formik';




export default function registerMeasuresScreen () {

    return (
        <View>
            <Text> Measure View</Text>
        </View>
    )
}